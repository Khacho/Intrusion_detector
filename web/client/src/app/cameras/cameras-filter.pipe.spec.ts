import { CamerasFilterPipe } from './cameras-filter.pipe';
import { } from 'jasmine';

xdescribe('CamerasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CamerasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
